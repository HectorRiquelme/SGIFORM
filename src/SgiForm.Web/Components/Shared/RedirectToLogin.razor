@inject NavigationManager Nav
@inject IConfiguration Config

@code {
    protected override void OnAfterRender(bool firstRender)
    {
        if (firstRender)
        {
            var pb = (Config["PathBase"] ?? "").TrimEnd('/');
            Nav.NavigateTo(pb + "/login", forceLoad: true);
        }
    }
}
