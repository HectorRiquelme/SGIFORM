@inject Microsoft.Extensions.Configuration.IConfiguration Config

<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <base href="@GetBaseHref()" />
    <link rel="stylesheet" href="app.css" />
    <link rel="stylesheet" href="SgiForm.Web.styles.css" />
    <link rel="icon" type="image/png" href="favicon.png" />
    <!-- Leaflet.js CSS (debe ir en head para evitar flash sin estilo) -->
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />
    <HeadOutlet @rendermode="InteractiveServer" />
</head>

<body>
    <Routes @rendermode="InteractiveServer" />
    <!-- Leaflet.js debe cargarse ANTES de blazor.web.js para que esté disponible en primer render -->
    <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
    <script src="js/sgi.js"></script>
    <script src="_framework/blazor.web.js"></script>
</body>

</html>

@code {
    string GetBaseHref()
    {
        var pb = Config["PathBase"] ?? "";
        if (string.IsNullOrEmpty(pb)) return "/";
        return pb.TrimEnd('/') + "/";
    }
}
